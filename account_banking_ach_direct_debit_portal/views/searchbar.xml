<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <template id="debit_portal_searchbar">
        <nav
            t-attf-class="searchbar-navbar py-0 mb-2 {{classes if classes else ''}} {{'mt-3 rounded' if breadcrumbs_searchbar else 'border-top-0' }}"
        >

            <div class="searchbar-left">
                <div class="d-none d-md-block">
                    <t t-if="breadcrumbs_searchbar">
                        <t
                            t-call="account_banking_ach_direct_debit_portal.portal_breadcrumbs"
                        />
                    </t>
                    <span
                        t-else=""
                        class="navbar-brand mb-0 h1 me-auto"
                        t-esc="title or 'No title'"
                    />
                </div>

                <div class="d-md-none">
                    <h2 class="header-title me-1" t-esc="title or 'No title'" />
                </div>

                <t t-out="0" />
            </div>

            <div
                id="navbar_content"
                class="ms-auto p-0 mb-3 mb-sm-0 mb-lg-0 mt-1 mt-md-0"
            >
                <form
                    t-if="searchbar_inputs"
                    class="o_portal_search_panel ms-lg-4 me-1"
                >
                    <div class="input-group w-100">
                        <button
                            type="button"
                            class="btn btn-secondary dropdown-toggle"
                            data-bs-toggle="dropdown"
                        />
                        <div class="dropdown-menu" role="menu">
                            <t t-foreach='searchbar_inputs' t-as='input'>
                                <a
                                    t-att-href="'#' + input_value['input']"
                                    t-attf-class="dropdown-item#{search_in == input_value['input'] and ' active' or ''}"
                                >
                                    <span t-out="input_value['label']" />
                                </a>
                            </t>
                        </div>
                        <input
                            type="text"
                            class="form-control form-control-sm"
                            placeholder="Search"
                            t-att-value='search'
                            name="search"
                        />
                        <button
                            class="btn btn-secondary o_wait_lazy_js search-submit"
                            type="submit"
                        >
                            <span class="fa fa-search" />
                        </button>
                    </div>
                </form>

                <div class="d-flex align-items-center justify-content-end">

                    <div
                        t-if="searchbar_sortings"
                        class="d-flex align-items-center justify-content-start"
                    >
                        <span class="small me-1 navbar-text">Sort By:</span>
                        <div class="btn-group">
                            <button
                                id="portal_searchbar_sortby"
                                data-bs-toggle="dropdown"
                                class="btn btn-secondary dropdown-toggle"
                            >
                                <t
                                    t-esc="searchbar_sortings[sortby].get('label', 'Newest')"
                                />
                            </button>
                            <div
                                class="dropdown-menu"
                                aria-labelledby="portal_searchbar_sortby"
                            >
                                <t t-foreach="searchbar_sortings" t-as="option">
                                    <a
                                        t-att-href="request.httprequest.path + '?' + keep_query('*', sortby=option)"
                                        t-attf-class="dropdown-item#{sortby == option and ' active' or ''}"
                                    >
                                        <span
                                            t-esc="searchbar_sortings[option].get('label')"
                                        />
                                    </a>
                                </t>
                            </div>
                        </div>
                    </div>

                    <div
                        t-if="searchbar_groupby"
                        class="d-flex align-items-center justify-content-start ms-2"
                    >
                        <span class="small me-1 navbar-text">Group By:</span>
                        <div class="btn-group">
                            <button
                                id="portal_searchbar_groupby"
                                data-bs-toggle="dropdown"
                                class="btn btn-secondary dropdown-toggle"
                            >
                                <t
                                    t-esc="searchbar_groupby[groupby].get('label', 'None')"
                                />
                            </button>
                            <div
                                class="dropdown-menu"
                                aria-labelledby="portal_searchbar_groupby"
                            >
                                <t t-foreach="searchbar_groupby" t-as="option">
                                    <a
                                        t-att-href="default_url + '?' + keep_query('*', groupby=option)"
                                        t-attf-class="dropdown-item#{groupby == option and ' active' or ''}"
                                    >
                                        <span
                                            t-esc="searchbar_groupby[option].get('label')"
                                        />
                                    </a>
                                </t>
                            </div>
                        </div>
                    </div>

                    <div
                        t-if="searchbar_filters"
                        class="d-flex align-items-center justify-content-start ms-2"
                    >
                        <span class="small me-1 navbar-text">Filter By:</span>
                        <div class="btn-group">
                            <button
                                id="portal_searchbar_filters"
                                data-bs-toggle="dropdown"
                                class="btn btn-secondary dropdown-toggle"
                            >
                                <t
                                    t-esc="searchbar_filters.get(filterby,searchbar_filters.get('all')).get('label', 'All')"
                                />
                            </button>
                            <div
                                class="dropdown-menu"
                                aria-labelledby="portal_searchbar_filters"
                            >
                                <t t-foreach="searchbar_filters" t-as="option">
                                    <a
                                        t-att-href="default_url + '?' + keep_query('*', filterby=option)"
                                        t-attf-class="dropdown-item#{filterby == option and ' active' or ''}"
                                    >
                                        <span
                                            t-esc="searchbar_filters[option].get('label')"
                                        />
                                    </a>
                                </t>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </nav>
    </template>
</odoo>
