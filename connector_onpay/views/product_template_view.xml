<odoo>
    <record id="view_product_template_onpay_inherit" model="ir.ui.view">
        <field name="name">product.template.onpay</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <field name="barcode" position="after">
                <field attrs="{'invisible':[('can_be_expensed','!=',True)],'required':[('can_be_expensed','=',True)]}" groups="connector_onpay.group_onpay_allow_export" name="product_onpay_id"/>
            </field>
        </field>
    </record>
</odoo>